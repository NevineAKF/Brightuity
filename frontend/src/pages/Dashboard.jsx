// Purpose: Dashboard page — living queue of pending tokenization requests as interactive cards.
// Each card: randomuser photo, encrypted doc ID, request ID, client name, country, asset type, asset value.
// State updates each session based on case lifecycle (pending → in_review → processed).
