# Purpose: KYC Guardian agent — plain Python, AI/ML API platform.
# Most sensitive agent: identity verification, sanctions/PEP screening, source-of-funds analysis.
# Can trigger a hard halt — no subsequent agents run if KYC fails.
